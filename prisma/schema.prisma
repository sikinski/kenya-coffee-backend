generator client {
    provider = "prisma-client-js"
}


datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

model User {
    id        Int      @id @default(autoincrement())
    name      String   @default("Имя пользователя")
    username  String   @unique
    password  String
    role      String   // "admin" или "user"
    createdAt DateTime @default(now())
    daily_tasks DailyTask[] // <-- обратная связь
    notes     Note[]    // 👈 добавили связь с заметками
}

model Task {
    id        Int      @id @default(autoincrement())
    text      String   // текст задачи
    order     Int      @default(autoincrement())
    createdAt DateTime @default(now())
    dailyTask DailyTask?
}

model DailyTask {
    id        Int      @id @default(autoincrement())
    date      DateTime
    done      Boolean  @default(false)
    userId    Int?
    user      User?    @relation(fields: [userId], references: [id])
    taskId    Int      @unique
    task      Task     @relation(fields: [taskId], references: [id])
}

model Note {
    id          Int      @id @default(autoincrement())
    text        String
    author_id   Int
    author      User     @relation(fields: [author_id], references: [id])
    created_at  DateTime @default(now())
    updated_at  DateTime @updatedAt
    topicId    Int?                               // связь с темой
    topic      NoteTopic? @relation(fields: [topicId], references: [id])
}

model NoteTopic {
    id     Int     @id @default(autoincrement())
    name   String  // название темы
    color  String  // цвет (hex, rgb или tailwind-class — решим сами)
    notes  Note[]
    created_at        DateTime @default(now())
}

model Receipt {
  id                 String   @id
  cashier_name       String
  discount           String?
  type               Int?
  customerInfo       Json?
  positions          Json?
  amount             Decimal
  calculationAddress String
  processedAt        String
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt
}

model NativeReceipt {
  id           String   @id @default(cuid())
  aqsiId       String?   @unique
  raw          Json
  processedAt  DateTime @db.Timestamp(6)
  processedAtRaw    String?
  createdAt    DateTime @default(now())
}

model StatsCache {
    id         String   @id @default(cuid())
    period     String   // 'day', 'week', 'month', 'quarter', или 'custom'
    from       DateTime
    to         DateTime
    revenue    Float // 💰 общая выручка за период
    avgCheck   Float // 📈 средний чек
    receiptsCount Int // 🧾 количество чеков
    dayRevenue Float // ☀️ выручка за день
    createdAt  DateTime @default(now())
    devices     String?
}


// revenue,        // 💰 общая выручка за период
//         avgCheck,       // 📈 средний чек
//         receiptsCount,  // 🧾 количество чеков
//         dayRevenue      // ☀️ выручка за день